"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.blendVideos = blendVideos;
var child_process_1 = require("child_process");
function getVideoDuration(videoPath) {
    return new Promise(function (resolve, reject) {
        var command = "ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 \"".concat(videoPath, "\"");
        console.log(command);
        (0, child_process_1.exec)(command, function (error, stdout, stderr) {
            if (error) {
                console.log("error: ".concat(error.message));
                reject("error: ".concat(error.message));
                return;
            }
            if (stderr) {
                console.log("stderr: ".concat(stderr));
                reject("stderr: ".concat(stderr));
                return;
            }
            resolve(parseFloat(stdout.trim()));
        });
    });
}
function generateFfmpegCommand(videoFiles, output, transition, transitionDuration) {
    return __awaiter(this, void 0, void 0, function () {
        var frameRate, fpsMap, filterComplex, audioTransitions, prevVideo, prevAudio, fadeDuration, lastOffset, index, duration, nextIndex, videoFade, fpsFade, audioFade, xFadeTransition, xFadeDuration, offset, finalCommand, error_1;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    _a.trys.push([0, 5, , 6]);
                    if (videoFiles.length < 2) {
                        console.log('Need at least two video files to create transitions.');
                        throw new Error('Need at least two video files to create transitions.');
                    }
                    if (transition instanceof Array && transition.length !== videoFiles.length) {
                        console.log('Transition array length should be equal to video files length');
                        throw new Error('Transition array length should be equal to video files length');
                    }
                    frameRate = '[0:v]settb=AVTB,fps=30/1[vfps0];';
                    fpsMap = " -map \"[vfps0]\" ";
                    filterComplex = '';
                    audioTransitions = '';
                    prevVideo = "[0:v]";
                    prevAudio = "[0:a]";
                    fadeDuration = transitionDuration;
                    lastOffset = 0;
                    index = 0;
                    _a.label = 1;
                case 1:
                    if (!(index < videoFiles.length - 1)) return [3 /*break*/, 4];
                    return [4 /*yield*/, getVideoDuration(videoFiles[index])];
                case 2:
                    duration = (_a.sent());
                    nextIndex = index + 1;
                    videoFade = "[vfade".concat(nextIndex, "]");
                    fpsFade = "[vfps".concat(nextIndex, "]");
                    audioFade = "[afade".concat(nextIndex, "]");
                    xFadeTransition = transition instanceof Array ? transition[index].transition : transition;
                    xFadeDuration = transition instanceof Array ? transition[index].duration : fadeDuration;
                    offset = lastOffset + duration - xFadeDuration;
                    lastOffset = offset; // Update last offset used for the next iteration
                    frameRate += "[".concat(nextIndex, ":v]settb=AVTB,fps=30/1").concat(fpsFade, ";");
                    filterComplex += "".concat(prevVideo, "[").concat(nextIndex, ":v]xfade=transition=").concat(xFadeTransition, ":duration=").concat(xFadeDuration, ":offset=").concat(offset).concat(videoFade, ";");
                    audioTransitions += "".concat(prevAudio, "[").concat(nextIndex, ":a]acrossfade=d=").concat(xFadeDuration).concat(audioFade, ";");
                    fpsMap += " -map \"".concat(fpsFade, "\" ");
                    prevVideo = videoFade; // Update previous video to current for the next iteration
                    prevAudio = audioFade; // Update previous audio to current for the next iteration
                    _a.label = 3;
                case 3:
                    index++;
                    return [3 /*break*/, 1];
                case 4:
                    // Append format setting to the last video fade label used and specify output labels for map
                    filterComplex += "".concat(prevVideo, "concat=n=").concat(videoFiles.length, ":v=1:a=1[vout];");
                    audioTransitions += "".concat(prevAudio, "aformat=sample_fmts=fltp:sample_rates=44100:channel_layouts=stereo[aout];");
                    finalCommand = "ffmpeg -y ".concat(videoFiles
                        .map(function (file, index) { return "-i \"".concat(file, "\""); })
                        .join(' '), " -filter_complex \"").concat(frameRate, " ").concat(filterComplex, "\" -map \"[vout]\" ").concat(fpsMap, " \"").concat(output, "\"");
                    return [2 /*return*/, finalCommand];
                case 5:
                    error_1 = _a.sent();
                    console.log('Failed to generate ffmpeg command:', error_1);
                    console.error('Failed to generate ffmpeg command:', error_1);
                    throw error_1;
                case 6: return [2 /*return*/];
            }
        });
    });
}
function blendVideos(videoPaths_1, output_1, transition_1) {
    return __awaiter(this, arguments, void 0, function (videoPaths, output, transition, transitionDuration, callback) {
        var command, error_2;
        var _this = this;
        if (transitionDuration === void 0) { transitionDuration = 0.5; }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    console.log('Error concatenating videos:');
                    _a.label = 1;
                case 1:
                    _a.trys.push([1, 3, , 4]);
                    return [4 /*yield*/, generateFfmpegCommand(videoPaths, output, transition, transitionDuration)];
                case 2:
                    command = _a.sent();
                    console.log(command);
                    (0, child_process_1.exec)(command, function (error, stdout, stderr) { return __awaiter(_this, void 0, void 0, function () {
                        return __generator(this, function (_a) {
                            if (error) {
                                console.log('Failed to process videos:', error);
                                callback(error, null);
                            }
                            else {
                                console.log('done');
                                callback(null, output);
                                return [2 /*return*/];
                            }
                            return [2 /*return*/];
                        });
                    }); });
                    return [3 /*break*/, 4];
                case 3:
                    error_2 = _a.sent();
                    console.log('Failed to process videos:', error_2);
                    if (callback)
                        callback(error_2, null);
                    return [3 /*break*/, 4];
                case 4: return [2 /*return*/];
            }
        });
    });
}
